import React from 'react';
import ReactDOM from 'react-dom';
import BookMyShowPrototype from './BookMyShowPrototype';
import './styles.css';

ReactDOM.render(
  <React.StrictMode>
    <BookMyShowPrototype />
  </React.StrictMode>,
  document.getElementById('root')
);
